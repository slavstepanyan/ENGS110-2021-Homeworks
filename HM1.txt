Steps to submit:Please answer to all questions below and submit the document with all the answers in github (can be a .txt file)Deadline: Sunday, Aug 29th, 23:591. Possible values set to store in 'char'Answer: char => 1 byte => -128 to 127ASCII vs. Unicode: https://www.youtube.com/watch?v=I-pQH_krD0M&t=1s--------------------------------------------------------------------------------------------------------------------2. What will print the following code?#include <stdio.h>int main(){    int a = 5;    while (a-- > 0) {        printf("%d\n", a);    }    return 0;}Answer:43210--------------------------------------------------------------------------------------------------------------------3. Can a C program be compiled or executed in the absence of a main()?Answer: No, if it is hosted environment--------------------------------------------------------------------------------------------------------------------4. Can I declare the same variable name to the variables which have differentscopes?void function1(){   int variable = 0;}void function(){   int variable = 5;}Answer: Yes, those will be local and work separate for each scope--------------------------------------------------------------------------------------------------------------------5. What will print the following code?void increment(int* i){    *i = *i + 1;}int main(){    int count = 7;    increment(&count);    printf("%d\n", count);    return 0;}Answer: 8--------------------------------------------------------------------------------------------------------------------6. What will print the following program?#include <stdio.h>int main(){    int x = 5641;    if (x & 1) {        printf("Even\n");    } else {        printf("Odd\n");    }    return 0;}Answer: output will be “Even” because & is a bitwise and operator. As 5641 and 1 are odd numbers their least significant bit is 1 so we have an output of 1 (true) and get an answer “Even”--------------------------------------------------------------------------------------------------------------------7. What will print the following program, if the sizeof(int) is equal 4?int main(){    int x[5];    printf("sizeof(int)=%ld, sizeof(x)=%ld", sizeof(int), sizeof(x));    return 0;}Answer: sizeof(int)=4, sizeof(x)=20--------------------------------------------------------------------------------------------------------------------8. What will print the following program, if as array size '7' is provided?int main(){    int s = 0;    printf("Enter array size: ");    scanf("%d", &s);    int* a = new int[s];    for (int i = 0; i < s; ++i) {        a[i] = i;    }    for (int i = 0; i < s; ++i) {        printf("%d\n", a[i]);    }    delete [] a;    return 0;}Answer:1234567--------------------------------------------------------------------------------------------------------------------9. What will print the following program?struct point{    int x;    int y;};int main(){    struct point p = {4, 5};    printf("x=%d, y=%d\n", p.x, p.y);    return 0;}Answer: x=4, y=5--------------------------------------------------------------------------------------------------------------------10. Can the union elements be of different sizes?union data{    char c;    int i;};Answer: Yes